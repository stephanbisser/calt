# Scan your manifest

Open a `declarativeAgent.json` (or any manifest in your `appPackage/`). CALT
will lint it automatically on save and underline rule violations in the editor.

You can also run **CALT: Scan Active Manifest** at any time to force a scan.
